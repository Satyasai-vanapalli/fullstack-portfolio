{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "dockerfile",
    "context": "."
  },
  "deploy": {
    "startCommand": "docker-compose up -d",
    "restartPolicyMaxRetries": 5
  }
}
